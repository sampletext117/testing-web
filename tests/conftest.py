import pytest

@pytest.fixture
def context():
    """
    Фикстура, возвращающая пустой словарь для хранения контекста сценария.
    Используется для передачи данных между шагами BDD.
    """
    return {}
